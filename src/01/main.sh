#!/bin/bash

echo $1

if [ -n "$1" ]; then
    if [[ "$1" =~ ^[0-9]+(\.[0-9]+)?$ ]]; then
        echo "Некорректный ввод. Параметр не должен быть числом."
    else
        echo "Правильно! Ваш параметр: $1"
    fi
else
    echo "Параметр не найден"
fi